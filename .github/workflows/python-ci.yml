name: Python CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Install uv
        uses: astral-sh/setup-uv@v8.2.0
        with:
          python-version: '3.13'
      - name: Install dependencies
        run: uv sync
      - name: Test with unittest
        run: uv run python -m unittest discover --verbose --start-directory backend
